#!/bin/bash

# script to run document processor in development mode
collector/venv/scripts/activate
# cd ..
# { set FLASK_ENV = "production" && set FLASK_APP="wsgi.py" && cd ./collector && flask run --host=0.0.0.0 --port=3005; }
#{ $env:FLASK_ENV = "production" && $env:FLASK_APP="wsgi.py" && cd ./collector && flask run --host=0.0.0.0 --port=3005; }
# wait -n
# exit $?

# powershell
# cd .\anything-llm\collector\
# venv/scripts/activate
# $env:FLASK_ENV = "production"
# $env:FLASK_APP="wsgi.py"
# flask run --host=0.0.0.0 --port=3005
